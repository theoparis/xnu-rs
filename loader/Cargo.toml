[package]
name = "loader"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.98"
publish = false

[lints]
workspace = true

[dependencies]
goblin.workspace = true
log.workspace = true
uefi = { workspace = true, features = ["alloc", "global_allocator", "logger"] }
